name: Availability API Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test availability
        run: |
          curl -sS --max-time 20 \
            "https://indian-railway-api.onrender.com/api/availability?train=17256&from=BMT&to=GNT&date=2026-09-26&class=SL" \
            -o response.json

          echo "=== RESPONSE ==="
          cat response.json
